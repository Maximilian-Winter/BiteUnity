module MainModule;

import System;
using System;

import UnitySystem;
using UnitySystem;
var cameraTransform;
var targetTransform;
var right;

sync
{
    cameraTransform = Camera.transform;
    targetTransform = TransformTarget;
    right = CSharpInterfaceCall(Vector3Instantiator);
}


    

    
while(true)
{
    sync
    {
        
        right.x = 1 * UnityDeltaTime();
        cameraTransform.LookAt(targetTransform);
        cameraTransform.Translate(right);
    }
}






