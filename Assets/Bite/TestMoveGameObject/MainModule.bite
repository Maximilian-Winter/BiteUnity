module MainModule;

import System;
using System;

import UnitySystem;
using UnitySystem;
var cameraTransform;
var targetTransform;
var right = CSharpInterfaceCall(Vector3Instantiator);

sync
{
    cameraTransform = Camera.transform;
    targetTransform = TransformTarget;
}


    

    
while(true)
{
    sync
    {
        
        right.x = 1 * UnityDeltaTime();
        cameraTransform.LookAt(targetTransform);
        cameraTransform.Translate(right);
    }
}






